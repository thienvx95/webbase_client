import { translations } from 'locales/translations';
import { _t } from 'utils/messages';

export const messages = {
  'en-GB': () => _t(translations.languages.English),
  'vi-VN': () => _t(translations.languages.Vietnamese),
};
